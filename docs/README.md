# IDOS 框架设计文档

日期：2026-09-22。适用仓库：`D:\CMakeCoder\IDOS`。

本组文档是基于当前源码的目标设计，供评审使用，不表示这些类和能力已经实现。本次仅新增文档，不移动、重命名或修改源码，也不套用 `D:\QtCoder\IDOS` 的实现结构。

## 阅读顺序

0. [工程介绍](IDOS-工程介绍.md)：当前源码的真实状态——技术栈、构建方式、模块划分、核心机制与已知问题。新加入者先读这篇。
1. [总体架构与实施方案](IDOS-架构设计方案.md)：设计边界、业务流程、关键取舍、迁移阶段与验收。
2. [总目录与类文件组织](IDOS-目录与类文件组织.md)：目标目录、类名、文件名、公共接口与现有文件映射。
3. [UML 与架构图](IDOS-UML设计图.md)：模块依赖、领域类、窗口类和导入时序。

## 已确定的设计约束

- 采用 C++17、Qt 5、CMake，沿用现有技术基础。
- 渲染只有 `src/render` 一个模块，数据转换、场景、交互都在其内部。
- 专业数据按 `core/data/well`、`core/data/grid`、`core/data/simulation` 分目录。
- 工程拥有业务对象；树、工况和窗口通过稳定 ID 引用对象。
- 工作区管理窗口，每个渲染窗口拥有独立显示状态。
- 保留现有 Provider 和通用树机制，逐步补齐工程、操作和显示闭环。
- 本设计不要求立即创建全部目录、空类或 DLL。

## 图的查看

UML 文档使用 Mermaid，包含类图和时序图；模块图使用 Mermaid 流程图表达依赖。可在支持 Mermaid 的 Markdown 预览中直接查看。图中未列出的成员不表示禁止添加；签名是职责示意，不是已经冻结的 SDK。

## 当前源码核对范围

已阅读根目录和各主要模块 CMake、Core 对象及工程、井/网格/工况、Provider 与导入协调、树扩展接口、主窗口、启动入口。当前渲染和部分扩展模块仍为占位；LAS 尚未实现完整解析。检查时仓库存在大量用户未提交修改，本次保留不动。

当前仓库未发现 `AGENTS.md` 或既有工作记录文档。前面对话提供的 QtCoder 规范仅作为设计参考：本方案明确提出按专业目录开放 API，区别于该规范的“仅模块根目录公开”规则；这不是对任何现有规范文件的静默修改。
